# G004-B current-tree correctness certification

Parent commit: `116b460e494f678ea89e6f90e457130bb1f6239c`
Parent tree: `3288131ada29b5cd2414fadc7db12d1e3d39ca90`
Upstream authority: `c7bef0ff14a13fd6ea52e3347da2c659383392de`

The retained G004-B evidence certifies these public API boundaries against
the scalar reference and exact candidate archive:

- `ValidateUTF8`: valid and malformed UTF-8 results match the scalar oracle.
- `ValidateUTF8WithErrors`: success length and first error position match.
- `CountUTF8`: valid code-point counts match the scalar oracle.
- `TrimPartialUTF8`: trailing partial sequence trimming matches the oracle.
- `Latin1LengthFromUTF8`: valid-input output length matches the oracle.
- `UTF16LengthFromUTF8`: valid-input output length matches the oracle.
- `UTF32LengthFromUTF8`: valid-input output length matches the oracle.

Local profiles: `darwin/arm64 GOEXPERIMENT=nosimd,runtimesecret` and `simd,runtimesecret`.
Remote profiles: `linux/amd64 GOAMD64=v1 GOEXPERIMENT=none` and `simd`.
Portable endian scope: compile-only for `linux/riscv64` and `linux/s390x`.
AVX2 OS-state scope: synthetic negative gate plus qualifying-host live execution.
Performance verdict: DEFERRED to G004-C.
Dispatch verdict: DEFERRED to G004-C.
